{% extends 'activity-base.twig' %}

{% block responses %}

    <div class="responses">
    {% set letters = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'] %}
    {% for rindex, response in question.responses %}
        <p class="response">
            <a href="#{{ qid }}_{{ rindex }}"><span>[ {{ letters[rindex]|upper }} ]</span></a>
            &nbsp;{{ response }}
        </p>
    {% endfor %}
    </div>

{% endblock %}

