/*
 * Footnotes
 * =========
 */

div.item.footnotes{
}

    div.item.footnotes ol {
    }

    div.item.footnotes li {
    }

    div.item.footnotes li p {
    }

    div.item.footnotes a.footnote-backref {
    }

    div.item.footnotes aside span {
        font-size: 0.7em;
    }

a.footnote-ref {
    font-size: 1em;
    font-weight: bold;
}

sup + sup::before {
    content: ','
}

a.footnote-backref {
    font-weight: bold;
}